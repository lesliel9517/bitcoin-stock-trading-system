system:
  mode: simulation  # simulation | live
  log_level: INFO
  data_dir: ./data

database:
  type: sqlite
  path: ./data/db/trading.db

cache:
  enabled: false
  type: redis
  host: localhost
  port: 6379
  db: 0
